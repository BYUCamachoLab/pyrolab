# default.yaml

nameservers:
  - default:
      host: localhost
      ns_port: 9090
      ns_bchost: localhost
      ns_bcport: 9091
      ns_autoclean: 0.0
      storage_type: memory

servers:
  - default:
      classname: Daemon
      host: localhost
      servertype: thread
